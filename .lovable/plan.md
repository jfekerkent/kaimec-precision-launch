In `src/pages/Index.tsx`, locate the hero background `<video>` element's inline `style` object and add `objectPosition: "center 30%",` directly after the existing `objectFit: "cover",` line. No other changes to the file or project.